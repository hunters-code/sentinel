{
  "name": "sentinel",
  "version": "0.1.0",
  "private": true,
  "description": "One-hour parametric crash insurance for BTC on DeepBook Predict (Sui)",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter @sentinel/web dev",
    "dev:keeper": "pnpm --filter @sentinel/keeper dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20"
  }
}
